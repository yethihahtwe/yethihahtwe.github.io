VPS set up changes

add -y for sudo apt install apache2 apache2-doc apache2-utils

sudo ufw status before anything about ufw. It is initially showing Status: inactive

replace with sudo ufw allow in "Apache" instead of "Apache Full"
replace with sudo ufw allow in "OpenSSH"

replace apache2 default page section with http://your_server_ip
apache2-default.png

add -y for sudo apt install mysql-server -y

add type altered password after mysql_secure_installation

After password strength, y for the following:

By default, a MySQL installation has an anonymous user,
allowing anyone to log into MySQL without having to have
a user account created for them. This is intended only for
testing, and to make the installation go a bit smoother.
You should remove them before moving into a production
environment.

Remove anonymous users? (Press y|Y for Yes, any other key for No) :

also y for disable root login remotely

y for remove test database

y for reload privilege table

add -y for sudo apt install php libapache2-mod-php php-mysql
add -y for sudo apt install php-curl php-json php-cgi php-gd php-mbstring php-xml php-xmlrpc php-zip

write more about sudo a2dissite 000-default.conf

sudo apache2ctl configtest will show:
Syntax OK

Write hello page test as http://server_ip

skip certbot for now. then add after DNS

add -y for fail2ban

remove ChallengeResponseAuthentication no

add -y for laravel php modules

sudo apt install git -y

add links to next and previous posts under each post

replace with post specific images

